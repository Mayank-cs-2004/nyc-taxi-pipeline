name: 'taxi_dbt'
version: '1.0.0'
config-version: 2

profile: 'taxi_dbt'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
data-paths: ["data"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  taxi_dbt:
    staging:
      materialized: view
    marts:
      materialized: table
